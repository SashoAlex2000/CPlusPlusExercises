#include <algorithm>
#include <cctype>
#include <cstring>
#include <iostream>
#include <sstream>
#include <stack>
#include <string>

int main() {

    std::string inputLine;
    std::getline(std::cin, inputLine);
    std::cout << inputLine << std::endl;
    int n;
    std::cin >> n;
    std::cout << n << std::endl;

    // for (int i = 0; i < 2; i++) {
    //     std::getline(std::cin, inputLine);
    //     std::cout << inputLine << std::endl;

    // }


}
